// src/components/AdPlaceholder.tsx

import React from 'react';

export const AdPlaceholder = () => {
  // This is a simple, invisible spacer that cannot cause rendering errors.
  return (
    <div className="w-full min-h-[250px]" />
  );
};
